[push]
        default = matching
[user]
	name = Sergey Sova
	email = i.am@lestad.net
	signingkey = C0E44D27
[core]
        excludesfile = /Users/ssova/.gitignore_global
	ignorecase = false
	editor = /usr/bin/vim
[alias]
        co = checkout
        ci = commit
        s = status
        br = branch
        h = log --pretty=format:\"%C(yellow)%h %C(green)[%cn] %C(cyan)%ad %Creset| %s %C(blue)%d\" --graph --date=short
        fp = fetch -p
        cim = commit -m
	l = log --oneline --graph --format='%C(yellow)%h %C(green)%cn %C(reset)%s'
[merge]
	tool = vimdiff

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[merge]
    conflictstyle = diff3
[color]
	ui = true
[commit]
	gpgsign = true
